package lambda;

import java.util.function.BiFunction;

/*public class Starter {
    public static void main(String[] args) {
        Starter s = new Starter();
        Person person =new Person();


        s.processHospital(person, 4, ((grade, days) -> 0.8 * 8 * grade * days));
        s.processAdditional(person, 8.5, ((grade, hours) -> 2.0 * hours * grade));

    }

    *//*public  void processAdditional (Person person, Double hours, BiFunction<Double, Double, Double> pd) {
        Double sum = pd.apply(person.getGradle(), hours);
    }
    public void processHospital (Person person, Integer days, BiFunction<Double, Integer, Double> ph) {
        Double sum = ph.apply(person.getGradle(), days);

    }*//*
}*/
